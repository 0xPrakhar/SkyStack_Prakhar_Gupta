services:
  - type: web
    name: eventify-api
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: CLIENT_URLS
        fromService:
          type: web
          name: eventify-web
          property: url
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 7d
      - key: DEFAULT_ADMIN_EMAIL
        value: admin@eventify.local
      - key: DEFAULT_ADMIN_PASSWORD
        value: Admin@12345

  - type: web
    name: eventify-web
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    pullRequestPreviewsEnabled: false
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: eventify-api
          property: url
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
